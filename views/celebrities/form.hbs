{{!-- {{#if book.isNew }}
  <form action="/books/create" method="post">
{{else}}
  <form action="/books/{{book.id}}/edit" method="post">
{{/if}} --}}

<form action="/celebrities/create" method="POST">

  <div class="form-group">
    <label for="name">name</label>
    <input type="text" class="form-control" id="name" name="name" value="{{celebrities.name}}">
  </div>

  <div class="form-group">
    <label for="occupation">occupation</label>
    <input type="text" class="form-control" id="occupation" name="occupation" value="{{celebrities.occupation}}">
  </div>

  <div class="form-group">
    <label for="catchPhrase">catchPhrase</label>
    <input class="form-control" name="catchPhrase" rows=3>{{celebrities.catchPhrase}}</input>
  </div>

  {{!-- <div class="form-group">
    <label for="author">user</label>
    <select class="form-control" name="user">
      {{#each users as |user|}}
        <option value="{{user.id}}">{{user.email}}</option>
      {{/each}}
    </select>
  </div> --}}

  <button type="submit" class="btn btn-primary">Submit</button>
</form>